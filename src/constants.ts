import { Time } from "./utils.ts";

export const ZERO_WIDTH_SPACE = "\u200b";

/** The time to live for a prompt text modal component. */
export const PROMPT_TEXT_VALUE_MODAL_TIMEOUT = Time.Minute * 2;
